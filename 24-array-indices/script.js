// Array indices

let marks = [77, 88, 99, 83, 91, 84, 80];
console.log(marks[0] = 100)  //Assigning a value to an array index replaces the existing element, and the assignment expression returns the assigned value.

console.log(marks[3])

