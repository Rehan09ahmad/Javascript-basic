// practice question 

let marks = [85, 97, 44, 37, 76, 60];

let sum = 0;
for(let val of marks ){
    sum += val;
}

let avg = sum / marks.length;
console.log(` avg marks of the class ${avg}`);





{
    let marks = [78, 89, 76, 99, 90, 81, 70, 60]

    let sum = 0;
    for(let val of marks){
        sum += val;
    }
    let avg = sum / marks.length;
    console.log(`avereg marks of the class ${avg}`);
}